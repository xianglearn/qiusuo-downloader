# 求索下载 · 技术组件清单

> 实现「求索下载」所需的全部技术组件、依赖、运行环境说明。
> 供新对话直接引用，避免重新调研。

---

## 一、外部引擎（必须与主程序同目录，可执行文件）

| 组件 | 版本参考 | 用途 | 来源 | 许可证 |
|---|---|---|---|---|
| `GoDingtalk` | v2.5.2 (windows_amd64) | 钉钉群直播完整源下载（解密/完整分片） | https://github.com/NAXG/GoDingtalk/releases | MIT |
| `mediago` | v0.3.0 | 多平台解析下载（92站点/103提取器，含钉钉/飞书/B站/抖音/慕课） | https://github.com/Sophomoresty/mediago/releases | The Unlicense |
| `ffmpeg` | 最新稳定版 | 分片合并、转码 MP4、音画同步 | FFmpeg 官方下载页 | GPL/LGPL |

> ⚠️ 发布时必须固定并随包附带的版本，勿随意更换，尤其 FFmpeg（高级参数需较新版本支持）。

---

## 二、主程序 Python 依赖（requirements-gui.txt）

```
customtkinter>=5.2.0      # GUI 框架
pillow>=10.0.0            # 图像处理（二维码/图标）
opencv-python-headless>=4.8.0  # 二维码识别预处理
pyzbar>=0.1.9             # 二维码解码
websocket-client>=1.8.0,<1.9.0  # 钉钉 LWP 只读接口
```

- 运行环境：Windows 10/11，Python 3.9+
- 从源码运行：`python -m pip install -r requirements-gui.txt && python gui_downloader.py`

---

## 三、上游源码结构（src/ 目录，11 个 py，约 9475 行）

| 文件 | 职责 | 改造影响 |
|---|---|---|
| `gui_downloader.py` | 主 GUI + 任务调度（2856行） | **需改**：加非钉钉 URL 分支、"整门课"开关 |
| `dingtalk_media.py` | 媒体解析/下载核心（1375行） | **需改**：classify_dingtalk_url 泛化 |
| `dingtalk_replay_extractor.py` | 群直播广场批量采集（1487行） | 保留（钉钉专用） |
| `browser_support.py` | 检测/唤起 Chromium 登录（846行） | 保留（钉钉） |
| `updater.py` | 自更新（785行） | 保留 |
| `replay_link_collector.py` | 一键采集小 GUI（590行） | 保留 |
| `session_support.py` | 登录会话存储/校验（416行） | 保留 |
| `dingtalk_rpc.py` | 钉钉 LWP 只读接口（415行） | 保留（钉钉） |
| `dingtalk_live_share.py` | 直播分享短链+观看密码（415行） | 保留 |
| `dingtalk_titles.py` | 标题提取（226行） | 非钉钉改用 MediaGo title |
| `qr_support.py` | 二维码识别增强（64行） | 保留 |

---

## 四、MediaGo 关键命令（多平台能力核心）

```bash
# 解析单个链接并输出 JSON 元数据（供主体读取标题/格式）
mediago --dump-json --no-progress -- "URL"

# 下载单个视频
mediago "URL"

# 下载整门课程/合集（多课时批量）★
mediago --yes-playlist "URL"

# 从浏览器读取已登录 Cookie（登录方案）★
mediago --cookies-from-browser chrome "URL"
# 或指定 Cookie 文件
mediago --cookies cookies.txt "URL"

# 并发分片下载
mediago -N 20 "URL"

# 模拟（只显示信息不下载）
mediago --simulate "URL"

# 列出支持的站点
mediago --list-extractors
```

---

## 五、GoDingtalk 关键命令（钉钉群直播专用）

```bash
# 下载单个钉钉回放
GoDingtalk.exe -url="https://n.dingtalk.com/...&roomId=XX&liveUuid=XX"

# 批量（urls.txt 每行一个，# 开头忽略）
GoDingtalk.exe -urlFile="urls.txt"

# 关键参数
-thread=10        # 下载线程
-saveDir=video/   # 保存目录
-cookies=...      # Cookie 文件
-login            # 强制重新登录
-chromePath=...   # Chrome 路径（自动查找）
-httpTimeout=30   # HTTP 超时
```

---

## 六、改造实现要点（给新对话）

1. **识别分流**：URL 判断——钉钉三类型（`n.dingtalk.com` 直播 / `shanji.dingtalk.com` 闪记 / `qr.dingtalk.com` 群文件）走原逻辑；其他 URL 走 MediaGo 通用解析。
2. **钉钉群直播**：仍 GoDingtalk 优先，MediaGo 兜底，密码标记时走 `_run_live_share`。
3. **其他平台**：`_run_mediago` 即可，把 `--dump-json` 输出的 title 当任务标题。
4. **登录**：其他平台调用 MediaGo 时传 `--cookies-from-browser chrome/edge`；读不到/失效 → 提示"请先在对应网站登录"。
5. **界面**：输入区去类型限制；加"课程批量采集"勾选框（接入 MediaGo 自带 `--yes-playlist`，自动采集整门课所有课时）→ 勾选则拼 `--yes-playlist`。
6. **验收**：按 `策划方案.md` 第九节清单逐项自查。

---

## 七、三引擎检查更新 + 确认替换（功能需求，必做）

> 用户明确要求：壳能检查三个引擎是否有更新，检测到后由用户决定是否更新，用户确认后自动下载并替换。

### 需求描述

1. **检查**：壳分别检查 GoDingtalk、MediaGo、FFmpeg 三个引擎的最新版本，与本机当前版本比较。
2. **提示 + 用户决定**：检测到新版时弹出提示（显示新版本号/大小），**等待用户确认**，不自动下载。
3. **确认后替换**：用户点"替换"后，壳下载新 exe → SHA-256 校验 → 备份旧文件 → 原子替换 → 失败自动回滚。
4. **保持单实例/互斥**：下载任务进行中不触发更新；替换时主程序退出相关进程。

### 实现建议

- **复用原 `updater.py` 框架**：它已实现"查 GitHub Release → 比较版本 → 下载 → SHA-256 校验 → 原子替换 → 回滚"。改造方向：
  - 由"只查主程序 Release"扩展为"分别查三引擎各自的 Release"（新增检查入口/路由）。
  - 按引擎平台下载对应资产（Windows → `*_windows_amd64.exe` / `*.zip`）。
  - GoDingtalk / MediaGo 走各自 GitHub Release（需 API 或 releases 页）。
  - **FFmpeg 特殊**：非 GitHub Release，走官网 ffmpeg.org 下载，可先放低优先级或手动替换。

### 引擎更新机制边界（已与用户确认）

- **引擎更新 ≠ 主程序更新**：绝大多数引擎更新（修 bug、加站点、加清晰度）只需**替换 exe**，主程序不用改。
- **主程序只在两类情况才更新**：① 用户主动改功能；② 引擎改参数/平台改协议导致需要跟着改。
- **平台换接口/改反爬会经常发生**，但**主要由引擎作者应对**，用户定期更新引擎 exe 即可，主程序很少需要动。
- 建议主程序设计"松耦合"（少依赖引擎细节参数），以扛住更新。

---

## 八、引擎更新历史（截至 2026-09-23）

| 引擎 | 最新版本 | 发布时间 | 项目引用版本 | 是否最新 |
|---|---|---|---|---|
| GoDingtalk | v2.5.2 | 2026-07-25 | v2.5.2 | ✅ 是 |
| MediaGo | v0.3.0 | 2026-07-04 | v0.3.0 | ✅ 是 |
| FFmpeg | 9.0.2 "Lei" | 2026-09-18 | 随附 | ✅ 建议用最新 |

> ⚠️ 三个引擎均在活跃更新（尤其 MediaGo 仍 v0.x 快速迭代、FFmpeg 近周更）。实施时到各 Release 确认最新版；FFmpeg 用最新稳定版但需实测 `-copyts -start_at_zero` 等参数支持。

---

## 九、合规红线

- 只下载有权访问的内容；遵守各平台 ToS 与版权法规。
- cookies.json / 登录会话严禁提交 Git。
- 三引擎各自许可证（MIT/Unlicense/GPL）在发布时遵守。
