# 求索下载 · 授权码与上线方案

> 本文档整理「求索下载」的授权码机制与上线方案。
> 目的：新对话（或豆包）下次拿到本方案，可直接照此实现，无需重新讨论。
> 整理时间：2026-09-24。本文档为**后续可选功能**，当前主程序仍可先无授权版使用。

---

## 一、授权码是什么（一句话）

**授权码 = 软件的"锁"，只有输入正确"钥匙"（授权码）才能解锁使用。**

核心三要素：码生成器（出钥匙）+ 软件内置验证（锁）+ 有效期（限时）。

---

## 二、授权码验证方式（两条路线）

| 方式 | 需要联网 | 需要服务器 | 安全性 | 成本 |
|---|---|---|---|---|
| **离线授权码** | ❌ 不需要 | ❌ 不需要 | 较低（易破解、防不了改时间） | 零成本 |
| **在线授权码** | ✅ 需要 | ✅ 需要（可免费平台） | 高（可封停/控制/防改时间） | 0~几十元/月 |

> 结论：起步阶段用**在线授权码（免费平台）**最划算；如只给少数人/朋友用，离线版即可。

---

## 三、方案一：离线授权码（零成本，简单）

### 原理
- **码生成器**用算法 A 生成一批码
- 软件内置**同一个算法 A** 验证
- **软件不用存任何码**——只要符合算法 A 的码（包括以后新随机生成的）都能通过

### 有效期做法
- 生成码时把"到期日期"**加密编进码串**
- 软件验证时解出到期日 → 与系统时间比对 → 没过期解锁，过期提示

### 关键点
- 生成器与软件验证**必须用同一算法**（一把钥匙开一把锁）
- 离线版防"改电脑时间"能力有限，需接受此局限

### 需要做的两件东西
1. **码生成器**：小脚本（Python/Excel），按算法批量生成码
2. **软件验证逻辑**：程序启动时校验用户输入的码 + 解出有效期

---

## 四、方案二：在线授权码（推荐，可免费起步）

### 原理
- 授权码**存在服务器/平台**，软件联网验证
- 服务器直接返回"有效 / 过期 / 无效"，可随时封停、控制、续期

### 是否需要买服务器：**不必买**（有免费替代）

| 子路线 | 操作 | 成本 |
|---|---|---|
| A. 用免费在线授权平台 | 注册平台 → 上传/生成码 → 软件调平台接口验证 | **0 元**（免费额度够个人用） |
| B. 自己买服务器 | 买云服务器 → 装后端 → 存码 → 软件联网验证 | 几十元/月起（规模大了才需要） |

> 推荐：**当前阶段走 A（免费平台）**，不买服务器；用户量大了再升级 B。

### 在线授权实操流程（免费平台路线）
```
① 注册一个免费在线授权平台
② 平台里：上传/生成授权码，设置有效期
③ 软件加几行代码：启动时联网调平台接口，验证码 + 查有效期
④ 完成：用户输入码 → 软件问平台"有效吗" → 平台回"有效/过期"
```

### 有效期做法
- 服务器存每个码的到期时间
- 软件每次验证，服务器直接告知是否过期
- 可随时改/续期，不用重发码

---

## 五、上线后的可选进阶（有效期类型）

| 类型 | 做法 |
|---|---|
| 按时间 | 年卡/月卡，到点失效 |
| 按次数 | 限定下载次数，用完失效 |
| 按功能 | 基础版/专业版，不同码解锁不同功能 |
| 绑设备 | 换电脑失效（机器码绑定） |

---

## 六、与打包的关系（重要）

- **授权码是"源码层逻辑"**，加授权 = **改源码 + 重新打包**，不是改 exe 本体。
- 已打包成 exe 后再加授权：**改源码加验证逻辑 → 重新打包** → 发布新版即可。
- 若确定以后要收费，建议**授权逻辑现在就加进源码**，避免返工。

---

## 七、加密（可选，配合授权码）

| 需求 | 做法 |
|---|---|
| 不想让人直接看 Python 源码 | **PyInstaller 打包成 exe**（已在做，挡大部分普通人） |
| 更防高手反编译 | Nuitka / Cython 编译核心模块 |
| 控制谁能用 | 授权码机制 |
| 防止引擎被单独拿走 | 较难（引擎需随包分发），一般不做 |

> 提醒：**没有绝对不可破解**，加密是提高门槛；**别去加密/篡改三个引擎 exe**（它们不是你的，尤其 FFmpeg 的 GPL）。

---

## 八、版权要点（配套）

- 壳部分（你的新增代码 + 界面）版权**归你**
- 上游 MIT 代码（DingTalkDownloader 主体 + GoDingtalk + MediaGo）著作权**仍归原作者**，需**保留 MIT 版权声明**
- FFmpeg 需遵守 GPL 条款；别用钉钉/B站官方 logo 当图标
- 除非彻底重写壳，否则"整个项目独占版权"做不到（引擎是别人的且无法重写）

---

## 九、给新对话的实现顺序建议

若新对话要落地授权码，建议按：
1. 先确认主程序功能版已能打包运行
2. 选路线：起步用**在线授权码（免费平台）**
3. 接入免费平台（注册 → 生成码 → 软件加联网验证 + 有效期提示）
4. 重新打包，测试：无码拦截 / 有效码解锁 / 过期码提示 / 不影响下载与更新
5. 合规：保留 MIT 版权声明、不篡改引擎

---

## 十、验收清单

- [ ] 软件启动时校验授权码（无码/错码/过期码有明确提示）
- [ ] 有效授权码可正常使用下载功能
- [ ] 有效期到期后正确拦截
- [ ] 不影响钉钉、多平台下载、批量采集、三引擎更新等原有功能
- [ ] 重新打包后 exe 能正常运行
- [ ] 保留上游 MIT 版权声明，未篡改三引擎
