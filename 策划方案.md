# 求索下载 · 项目策划方案

> 本方案由豆包与用户于 2026-09-23 讨论整理。
> 目的：给新对话提供完整、可执行的实现依据，使新对话只需按本方案直接落地，无需重新调研。

---

## 一、项目定位

基于开源项目 **DingTalkDownloader**（https://github.com/ULing19/DingTalkDownloader）改造的下载器，项目命名为 **「求索下载」**。

**核心目标：**
- 保留原钉钉下载全部功能（单个直播、群批量采集、闪记、群文件/钉盘）
- **新增多平台下载能力**（B站、抖音、慕课等 90+ 平台）
- 采用 **两条路线** 架构，钉钉功能不降级

**重要说明：** 本方案是"主体挂载多个引擎"的架构改造，不是从零开发。

---

## 二、架构总览（两条路线）

```
求索下载（主体：GUI + 登录 + 调度 + 校验）
│
├── 路线① 钉钉专用链路（保留原功能）
│    ├── 单个群直播链接 → GoDingtalk（首选，完整源）
│    ├── 群直播广场批量采集 → dingtalk_replay_extractor（原样保留）
│    ├── 闪记 / 群文件·钉盘 → MediaGo
│    └── 登录：原钉钉 CDP 授权（保留）
│
└── 路线② 其他平台链路（新增，走 MediaGo）
     ├── 单链接下载 → MediaGo
     ├── 课程/合集批量 → MediaGo --yes-playlist
     └── 登录 → MediaGo --cookies-from-browser（读浏览器已登录态）
```

---

## 三、三个外部引擎（挂载式组件）

| 引擎 | 作用 | 语言 | 许可证 | 来源 |
|---|---|---|---|---|
| **GoDingtalk** | 钉钉群直播完整源（解密/完整分片） | Go | MIT | https://github.com/NAXG/GoDingtalk |
| **MediaGo** | 多平台通用解析 + 下载（92站点/103提取器，含钉钉/飞书/B站/抖音/慕课等） | Go | The Unlicense | https://github.com/Sophomoresty/mediago |
| **FFmpeg** | 分片合并、转码封装 MP4、音画同步 | C | GPL/LGPL | FFmpeg 官方 |

**关键事实：**
- 三者均为独立可执行文件，主体通过**命令行调用**它们，非编译集成。
- MediaGo 自身已支持 92 个平台，是"多平台能力"的真正来源；改多平台=让主体把链接交给 MediaGo 通用解析。
- 发布时 4 个文件（主体 + 三引擎）必须同目录。

---

## 四、需要改动的部分（按工作量）

### 基本不用动 ✅
- 三个引擎本体（本来就是多平台就绪）
- 钉钉的 GoDingtalk 链路、群批量采集（`dingtalk_replay_extractor.py`）
- 钉钉登录/会话（`session_support.py`、`browser_support.py` 钉钉部分）

### 主体层需要改 ⚠️
| 模块 | 现在（钉钉专用） | 改造为 |
|---|---|---|
| `dingtalk_media.py` 的 `classify_dingtalk_url` | 只认 3 种钉钉链接 | 去钉钉校验，识别到钉钉走原链，其他交给 MediaGo 通用解析 |
| `gui_downloader.py` 任务分发 `_run_one` | 按钉钉类型分流 | 加分支：非钉钉链接统一走 MediaGo；钉钉直播仍走 GoDingtalk 优先 |
| `dingtalk_titles.py` 标题提取 | 钉钉字段专用 | 非钉钉链接改用 MediaGo 输出的 title 字段 |
| 登录提示 | 钉钉专用 | 非钉钉走 `--cookies-from-browser`，失效时提示"请先在该网站登录" |

### 可删除/禁用 🔄
- `dingtalk_rpc.py`、`dingtalk_replay_extractor.py` 的钉钉采集 → 仅在钉钉群批量时启用

### 界面层（小改）
- 输入区从"钉钉三类链接"改为通用 URL 输入
- 增加"课程批量采集"开关（透传 `--yes-playlist`）——这是接入 MediaGo 自带能力，非新增引擎功能
- 任务列表、日志、进度基本沿用

---

## 五、关键决策点（已与用户确认）

1. **采用两条路线**：同时保留 GoDingtalk（钉钉）+ MediaGo（多平台），钉钉功能不降级。
2. **钉钉群批量采集功能保留**：`dingtalk_replay_extractor` 原样保留，不影响多平台改造。
3. **课程批量采集**：能力是 MediaGo 自带的 `--yes-playlist`（能自动采集整门课所有课时），界面只需加开关透传即可，无需写核心逻辑。**这是"接入"而非"新增引擎能力"。**
4. **各平台登录各管各的**：钉钉走原授权；其他平台走 `--cookies-from-browser` 读浏览器已登录态 + 失效提示。**不存在一套通用登录**。
5. **B站类课程（分多课时）**：MediaGo `--yes-playlist` 即可自动采集整门课全部课时。
6. **三引擎检查更新 + 确认替换**：壳检查三引擎是否有新版，提示用户决定，确认后自动下载并替换（详见技术组件清单第七节）。
7. **引擎更新 ≠ 主程序更新**：绝大多数引擎更新只需替换 exe，主程序不用改；主程序只在"主动改功能"或"引擎改参数/平台改协议"时才更新。
8. **平台换接口/改反爬会经常发生，但主要由引擎应对**：用户定期更新引擎 exe 即可，主程序很少需要动。

---

## 六、功能变更一览（相对原版 DingTalkDownloader）

### 新增功能
| 功能 | 说明 |
|---|---|
| **多平台下载** | 支持 B站、抖音、慕课等 90+ 平台（经 MediaGo） |
| **三引擎检查更新 + 确认替换** | 检查 GoDingtalk/MediaGo/FFmpeg 是否有新版，提示用户决定，确认后自动下载替换 |

### 调整的功能
| 功能 | 原版（钉钉专用） | 调整为 |
|---|---|---|
| **URL 识别分流** | 只认钉钉 3 类链接 | 钉钉走原逻辑，其他 URL 交给 MediaGo 通用解析 |
| **任务分发** | 按钉钉类型分流 | 加"非钉钉 → 统一走 MediaGo"分支 |
| **登录** | 钉钉专属授权 | 钉钉走原授权；其他平台走 `--cookies-from-browser` 读浏览器登录态 |
| **标题提取** | 钉钉字段专用 | 非钉钉链接改用 MediaGo 输出的 title |
| **界面输入** | 限钉钉三类链接 | 改为通用 URL 输入 |
| **课程批量采集** | 引擎 MediaGo 自带该能力，但原界面无入口 | 界面加开关透传 `--yes-playlist`，接入自动采集整门课所有课时 |

### 保留未动的功能
- 钉钉单个直播（GoDingtalk 完整源）
- 钉钉群直播广场批量采集
- 钉钉闪记、群文件/钉盘
- 观看密码流程（钉钉直播分享密码）
- 三引擎本体、下载完整性校验（MP4 音画检查）

---

## 七、发布/合规要求

- 发布时附带三个引擎 exe，并遵守各自许可证（MIT / Unlicense / GPL）。
- 仅供下载**有权访问**的内容，遵守各平台条款与版权法规；严禁盗版传播。
- 登录会话（cookies.json）严禁提交 Git 或公开分享。
- 钉钉仍走"只处理本人有权观看/下载的内容"原则。

---

## 八、目录结构规划

```
求索下载/
├── README.md            # 项目说明（本方案的简版）
├── 策划方案.md           # 本文件：完整实现依据
├── src/                 # 上游 DingTalkDownloader 源码（改造起点）
│   ├── gui_downloader.py
│   ├── dingtalk_media.py
│   ├── ...（11 个 py + 依赖文件）
└── 技术组件清单.md        # 三引擎 + Python 依赖 + 部署要求
```

---

## 九、实现验收清单（新对话据此自查）

- [ ] 钉钉单个直播链接下载，与原版行为一致（GoDingtalk 完整源）
- [ ] 钉钉群直播广场批量采集，功能保留
- [ ] 钉钉闪记、群文件/钉盘下载，行为一致
- [ ] B站等非钉钉链接可下载
- [ ] B站整门课程（多课时）可课程批量采集（--yes-playlist）
- [ ] 非钉钉平台登录：读浏览器登录态 + 失效提示
- [ ] 界面支持通用 URL 输入 + "课程批量采集"开关
- [ ] 三引擎检查更新 + 确认替换：能查三引擎版本、提示用户决定、确认后下载替换
- [ ] 三个引擎 exe 同目录，缺任一引擎有清晰提示
