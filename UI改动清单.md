# 求索下载 · UI 改动清单（给 UI 设计对话）

> 本清单只列出：改造后旧界面上**需要新增的控件** 和 **需要改文案的控件**。
> 不动的控件、布局、整体风格不在本清单范围（旧界面其余部分原样保留）。

---

## 一、需要新增的控件（共 1 个）

### 1. 「批量采集」勾选框

- **类型**：复选框（Checkbox / Checkbutton）
- **位置建议**：左侧「导入链接 / 二维码」输入区下方，靠近「一键获取已打开群回放」按钮区域（二者都属"采集类"，放一起便于理解；但二者功能不同）
- **文案**：`批量采集`
- **默认状态**：未勾选
- **提示文字**（tooltip / 说明，可选）：`下载整门课/合集的所有课时（如一门课的一、二、三……课时）`
- **功能语义**：勾选后，对当前要下载的链接启用 `--yes-playlist`，顺着链接把整门课/合集所有课时一起下载。**不是扫描当前页面。**
- **可用性**：仅在输入了非钉钉平台的课程/合集链接时有意义；钉钉链接或普通单视频勾选后无副作用（引擎自行处理）。

---

## 二、需要改文案的控件（共 1 个）

### 2. 导入区说明文字（改文案）

- **原文案**：`支持群回放、直播分享短链、闪记和群文件；观看密码在下载时按需输入。`
- **建议改为**：`支持钉钉回放/闪记/群文件，以及 B站、抖音、慕课等 90+ 平台；观看密码在下载时按需输入。`
- **原因**：URL 识别已从钉钉专用改为通用，说明文字需同步提示"也支持其他平台"。

---

## 三、功能不变、但注意文案/状态的控件（逻辑变了，控件不用加）

以下控件**界面位置不动**，但建议在设计时留意其功能内涵变化：

| 控件 | 变化说明 |
|---|---|
| **重新登录**（底部） | 逻辑扩展：钉钉走原授权，其他平台读浏览器登录态；按钮本身不变 |
| **检查更新**（底部） | 功能从"只查主程序"扩展为"查三引擎 + 确认后替换"；按钮本身不变 |
| **解析到任务列表** | 逻辑加"非钉钉链接分流"，按钮不变 |
| **任务列表** | 标题提取逻辑变（其他平台取 MediaGo title），界面不变 |

---

## 四、明确不改的控件（旧界面其余全部）

- 保存目录 + 浏览
- 分片线程 / 同时下载
- 导入文本 / 导入二维码 / 清空
- 一键获取已打开群回放（钉钉群批量采集，保留）
- 全选 / 全不选
- 开始下载 / 停止 / 打开保存目录 / GitHub仓库
- 总进度条 / 日志区
- 下载任务列表（勾选框等）

---

## 五、给 UI 设计对话的一句话总结

> 旧界面只**新增一个「批量采集」勾选框** + **改一句导入区说明文案**，其余所有控件和布局原样保留。
